#include "main.h"
/**
 * parent_alphabet - print in lowercase alphabet
 *
 * Return: return 0
 */
void parent_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
